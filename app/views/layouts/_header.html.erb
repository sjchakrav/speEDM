    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
           <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>

          <a class="brand goTop" href="/"><img src = "/assets/Headphones50.png/" width="30" height="30"></a>
          <div class="nav-collapse pull-right">
            <ul class="nav">
              <% if current_user %>
                <li>
                     <%= form_tag "/search", :method => 'post', :class => 'navbar-form' do %>
                       <p>
                         <%= text_field_tag "search[name]", params.has_key?("search") && params[:search].has_key?("name") ? params[:search][:name] : "", :placeholder => "Search artists..." %>
                         <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
                           <i class="icon-search"></i>
                         <% end %>
                       </p>
                     <% end %>
                </li>
              <% end %>
              <li><%= link_to "Home", root_path %></li>
              <% if current_user %>
                <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">My Account | <%="#{current_user.name} " %> <b class="caret"></b></a>
                  <ul class="dropdown-menu" role="menu">
                    <li> <%= link_to "Artists Following", user_path(current_user.id) %> </li>
                    <li> <%= link_to "Log Out", "/signout" %></li>
                  </ul>
                </li>
              <% else %>
                <li><%= link_to "Login with Spotify", "/auth/spotify"%></li>
              <% end %>
              </li>
            </ul>
          </div>  
        </div>  
      </div>
    </div>