<strong>Welcome, <%= @user.name %>.</strong>
<p>
<br>
<font size = "3">
 To start discovering new music, search for your favorite artists using the search bar at the top and then you'll be directed to the artist's profile. Click the "follow" button if you'd like to receive new updates about the artist and their tracks. All artists you're following will be displayed in the user dashboard as well as new tracks added on their relevant Spotify and Soundcloud profiles.</font>
<br>
<br>
<br>
  <strong>Artists being followed:</strong><br>
  <% @user.artists.each do |artist| %>
    <%= link_to artist.name, artist %><br>
  <% end %>
</p>

<p>
  <strong>Recent tracks from all artists from Spotify:</strong><br>
  <% @spotify_tracks.each do |track| %>
    <a href = "<%= track.url %>"><%= image_tag(track.image_url, size: "50x50") %> <%= track.artist.name %> - <%= track.name %> - Released: <%= track.release_date %></a><br>
  <% end %>
</p>

<%= page_entries_info @spotify_tracks %>
<%= will_paginate @spotify_tracks, :param_name => 'spotify_page' %>

<p>
  <strong>Top tracks from all artists from SoundCloud:</strong><br>
  <% @soundcloud_tracks.each do |track| %>
    <a href = "<%= track.url %>"><%= image_tag(track.image_url, size: "50x50") %> <%= track.name %> - Released: <%= track.release_date %></a><br>
  <% end %>
</p>

<%= page_entries_info @soundcloud_tracks %>
<%= will_paginate @soundcloud_tracks, :param_name => 'soundcloud_page' %>